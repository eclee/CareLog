# 升級至 1.1.0

1. 停止服務。
2. 備份 `carelog.db`、`uploads/` 或完整 `data/`。
3. 更新程式碼並重新安裝依賴。
4. 執行 `CARELOG_START_SCHEDULER=0 flask --app app init-db`。
5. 啟動服務並依序檢查登入、填報、儀表板、帳號管理及報表。

1.1.0 未刪除或重新命名既有資料表。新增長輩的生日預設值只影響新建立的資料，不會修改既有長輩生日。

既有使用者語言若為 `zh` 或 `id` 會直接沿用；管理者可在使用者管理中改為 `vi`、`fil` 或 `th`。
